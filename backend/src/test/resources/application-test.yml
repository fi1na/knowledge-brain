spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/knowledgebrain
    username: knowledgebrain
    password: knowledgebrain
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379
